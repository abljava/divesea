# DiveSea - NFT Marketplace

Адаптивный веб-сайт NFT маркетплейса с современным дизайном и интерактивными компонентами.

## Описание проекта

DiveSea - это современная платформа для открытия, создания и продажи NFT. Проект включает в себя:

- **Hero секция** с анимациями и статистикой
- **NFT слайдер** с бесконечной прокруткой, поддержкой свайпов и drag-and-drop
- **Skeleton screens** для улучшенного UX при загрузке данных
- **Обработка ошибок** с возможностью повторной загрузки
- **CTA секция** для привлечения пользователей
- **Адаптивный дизайн** для всех устройств (mobile, tablet, desktop)

## Технологический стек

- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **Redux Toolkit** - управление состоянием
- **SCSS** - стилизация
- **GSAP** - анимации
- **CoinGecko API** - данные NFT

## Установка и запуск

### Требования

- Node.js 18+ 
- npm или yarn

### Локальный запуск

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd divesea
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev-сервер:
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Предпросмотр production сборки

```bash
npm run preview
```

## Запуск через Docker

### Сборка образа

```bash
docker build -t divesea .
```

### Запуск контейнера

```bash
docker run -p 8080:80 divesea
```

Приложение будет доступно по адресу `http://localhost:8080`

## Доступные команды

- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка для production
- `npm run preview` - предпросмотр production сборки
- `npm run lint` - проверка кода линтером

## Структура проекта

```
divesea/
├── src/
│   ├── components/      # React компоненты
│   │   ├── common/      # Общие компоненты (SocialLinks)
│   │   ├── Header/      # Шапка сайта
│   │   ├── Hero/        # Главная секция с hero изображениями
│   │   ├── NFTSlider/   # Слайдер NFT с карточками и skeleton
│   │   ├── CTA/         # Секция призыва к действию
│   │   └── Footer/      # Подвал сайта
│   ├── store/           # Redux store и слайсы
│   │   ├── api/         # API запросы (CoinGecko)
│   │   └── slices/      # Redux слайсы (nftSlice)
│   ├── hooks/           # Кастомные хуки (useCountdownTimer)
│   ├── utils/           # Утилиты (генерация данных)
│   ├── types/           # TypeScript типы и интерфейсы
│   └── assets/          # Статические файлы
│       ├── images/      # Изображения (логотип, иконки, hero)
│       └── styles/      # SCSS стили (main, variables)
├── public/              # Публичные файлы (изображения для NFT карточек)
└── dist/                # Собранные файлы (генерируется)
```

## Особенности

### Производительность
- ⚡ Быстрая загрузка благодаря Vite
- 🖼️ Lazy loading для изображений ниже fold
- 🎯 Приоритетная загрузка критичных изображений (Hero)
- 🔄 Оптимизация через асинхронное декодирование изображений

### UX/UI
- 🎨 Адаптивный дизайн для всех устройств (mobile, tablet, desktop)
- 💀 Skeleton screens для плавной загрузки данных
- ⚠️ Обработка ошибок с возможностью повторной загрузки
- 🎭 Плавные анимации с GSAP

### Функциональность
- 📱 Поддержка touch-событий и drag-and-drop в слайдере
- 🔄 Бесконечная прокрутка NFT слайдера
- ⏱️ Работающий таймер обратного отсчета на каждой карточке
- 🌐 Интеграция с CoinGecko API для получения данных NFT

### Технологии
- 🎯 TypeScript
- 🎨 Модульные SCSS стили
- 📦 Redux Toolkit

## Лицензия

MIT
